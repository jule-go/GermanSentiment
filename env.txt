check https://packaging.python.org/en/latest/guides/installing-using-pip-and-virtual-environments/
and https://discuss.python.org/t/changing-cache-directory-in-a-persistent-way/5693
and https://pip.pypa.io/en/stable/topics/caching/

use the following to see where the cache is located: >>> pip cache dir
originally this is /home/users1/godberja/.cache/pip
>>> export PIP_CACHE_DIR=/mount/studenten-temp1/users/godberja/Cache/

use the following to delete cache of huggingface models:
>>> huggingface-cli delete-cache

Create environment:
>>> python3.8 -m venv .cslp_proj_env

to activate: 
>>> source /mount/studenten-temp1/users/godberja/.cslp_proj_env/bin/activate

to deactivate:
>>> deactivate


Installed packages:
>>> python -m pip install huggingface transformers
>>> python -m pip install ipykernel
>>> python -m pip install datasets
>>> python -m pip install huggingface_hub["cli"]
>>> python -m pip install -U deep-translator
>>> python -m pip install torch torchvision torchaudio
>>> python -m pip install adapters
>>> python -m pip install scikit-learn
>>> python -m pip install matplotlib

Other requirements:
>>> 